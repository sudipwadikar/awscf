pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation delete-stack --stack-name ec2sg"
              }
             }
            }
            }
